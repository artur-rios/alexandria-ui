name: alexandria_lints
description: "The project's own analyzer rules: the layering rule IR-02 requires."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  analyzer: ^8.4.0
  analyzer_plugin: ^0.13.4
  custom_lint_builder: ^0.8.1
